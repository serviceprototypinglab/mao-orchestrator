docker run -it --network host -v /var/run/docker.sock:/var/run/docker.sock -v /home/panos/newconfig:/home/panos/newconfig -v /home/panos/audit_temp:/home/panos/audit_temp -p 8080:8080 5a0090dd1169
# To build a network that can be access by additional containers
docker network create mao --driver bridge
# To rebuild the images
docker-compose build
Request needs to escape slashes
{
	"key":"insight\/spike\/sonap"
}
# Write raw data to cluster so they get saved to datasets
{
	"key":"raw\/newtest\/file7",
	"value":{
		"iam":"groot"
	}
}
